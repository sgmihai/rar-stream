[package]
name = "rar-hex"
version = "0.2.0"
edition = "2021"
description = "CLI tool to read byte ranges from files inside RAR archives (local or HTTP) and display as hex"

[[bin]]
name = "rar-hex"
path = "src/main.rs"

[dependencies]
rar-access = { path = "../rar-access" }
